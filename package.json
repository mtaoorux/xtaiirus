{
  "name": "cors-proxy",
  "version": "1.0.0",
  "description": "A minimal proxy that only adds CORS headers to allowlisted upstream URLs.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-fetch": "^2.7.0"
  }
}
